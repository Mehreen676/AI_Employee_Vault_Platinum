# AI Employee Vault – Platinum Tier Dashboard
# Copy this file to .env.local and fill in your values

# FastAPI backend URL (no trailing slash)
# Local development:
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# HuggingFace Spaces backend (production default, also in vercel.json):
# NEXT_PUBLIC_BACKEND_URL=https://mehreenasghar5-ai-employee-vault-backend.hf.space

# Vercel deployment — vercel.json sets this automatically; override in dashboard if needed.
