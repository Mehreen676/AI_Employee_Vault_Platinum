# AI Employee Vault – Platinum Tier Dashboard
# Copy this file to .env.local and fill in your values

# FastAPI backend URL (no trailing slash)
# Local development:
NEXT_PUBLIC_BACKEND_URL=http://localhost:7860

# HuggingFace Spaces deployment example:
# NEXT_PUBLIC_BACKEND_URL=https://<org>-<space-name>.hf.space

# Vercel deployment — set this in Vercel dashboard → Project → Settings → Environment Variables
