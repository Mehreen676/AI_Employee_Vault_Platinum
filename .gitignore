# Secrets
.env
credentials.json
token.json
*.pem
*.key

# Python
__pycache__/
*.pyc
venv/

# Runtime artifacts (do not commit)
vault/Logs/*.json
vault/Done/*.json
vault/Pending_Approval/*.json
vault/In_Progress/**
vault/Needs_Action/**
vault/Updates/**
vault/Deferred/**
vault/Retry_Queue/**
history/*.json
Dashboard.md

# Briefings (generated runtime output)
Briefings/*.md

# Rate limiter state (runtime counter — not a source artifact)
vault/Logs/rate_limit_state.json

# Claude IDE settings
.claude/

# Node / Next.js build artifacts
frontend_dashboard/.next/
frontend_dashboard/node_modules/
frontend_dashboard/package-lock.json
frontend_dashboard/next-env.d.ts

# Evidence runtime output
Evidence/JUDGE_PROOF.md

# OS
.DS_Store
Thumbs.db

# HuggingFace Space backend (separate git repo — pushed independently)
hf_space_backend/
